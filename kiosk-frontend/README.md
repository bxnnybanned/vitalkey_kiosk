# VitalKey Frontend (React + Vite)

## Run locally (frontend-only)

```powershell
npm install
npm run dev
```

Open: `http://localhost:5173`

Current mode: frontend-only (no backend/database required).
